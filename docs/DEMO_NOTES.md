# Demo Notes

`docs/demo.gif` is a placeholder media file to keep README embedding stable.

Before final submission, replace it with a short 20-40 second recording that shows:

1. Server startup.
2. `POST /reset` for a hard task.
3. One or two `POST /step` calls.
4. `GET /grader` and `GET /baseline` outputs.

Recommended naming: keep the final file path as `docs/demo.gif` so README links do not change.
